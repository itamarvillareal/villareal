-- Carga dos dados das planilhas (Cadastro Pessoas .xls, import clientes .xlsx, imoveis .xlsx)
-- não é versionada aqui (ficheiros binários e paths por máquina).
-- Ordem: pessoas → clientes → imóveis — propriedade `vilareal.import.batch.enabled=true`
-- e script `scripts/run-import-planilhas-batch.sh`.
SELECT 1;
