# Usado em `vite build` (ex.: Vercel). Força o app sem tela de login.
# Variáveis no painel da Vercel têm prioridade: para voltar a exigir login,
# remova esta linha ou defina VITE_SKIP_API_AUTH=false e VITE_REQUIRE_API_AUTH=true.
VITE_SKIP_API_AUTH=true
